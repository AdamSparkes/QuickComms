<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Start a New Message</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <% include("partials/header", { user: { username: sender } }) %>
    <div class="main-content">
        <h1>Select a User to Message</h1>
        <ul>
            <% users.forEach(user => { %>
                <li><a href="/chat/<%= user.username %>"><%= user.username %></a></li>
            <% }) %>
        </ul>
    </div>
</body>
</html>